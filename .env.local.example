# Optional: copy this file to .env.local if you prefer keeping the JSON
# outside the repository. If you place the JSON directly in
# local/credentials/service-account.json, no env file is needed.

GOOGLE_APPLICATION_CREDENTIALS=/Users/your-user/.config/gcp/workstatus-export.json
